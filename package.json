{
  "scripts": {
    "build": "esbuild ./src/main.ts ./src/popup.ts --outdir=./dist --minify"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "esbuild": "^0.15.12"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.200"
  }
}
